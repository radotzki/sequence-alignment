{
  "name": "sequence-alignment",
  "version": "1.0.0",
  "description": "Find the optimal alignment between two sequences",
  "main": "config.js",
  "scripts": {
    "deploy": "git checkout gh-pages && git rebase master && git push && git checkout master",
    "start": "node_modules/.bin/live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/radotzki/sequence-alignment.git"
  },
  "author": "@radotzki",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/radotzki/sequence-alignment/issues"
  },
  "homepage": "https://github.com/radotzki/sequence-alignment#readme",
  "devDependencies": {
    "live-server": "^0.9.2"
  }
}
